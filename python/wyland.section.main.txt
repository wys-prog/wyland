int main() {
  if (false) { something; }
  else if (true) { why note; }
  else { ok.. }

  while (a condition) {
    // do something
  }

  for (i; limit) {
    // do whatever 
  }

  call(myArguments);
}

types (and sizes): int (4 bytes), (u)long (4 bytes), (u)char(1 bytes), bool (1 bytes), 